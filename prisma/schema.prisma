generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Order {
  id              Int      @id @default(autoincrement())
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  status          String   @default("new")

  firstName       String
  lastName        String
  phone           String
  email           String
  address         String
  city            String
  zip             String

  waterType       String
  bottleSize      String
  bottlesPerDelivery String
  frequency       String @default("every-2-weeks")
  needsCooler     String

  notes           String   @default("")
}
